server:
  port: 9090
spring:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
  datasource:
    url: jdbc:mysql://localhost:3307/atividade_jwt
    username: root
    password: 3307
    driverClassName: com.mysql.cj.jdbc.Driver